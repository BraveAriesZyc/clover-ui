# 维护指南

## 监控指标
- 站点可用性（HTTP 2xx 比例）
- 构建与类型检查通过率
- 依赖更新与安全审计

## 常见问题排查
- 全局组件未识别：检查 `env.d.ts` 类型引用与 Volar 插件
- 图标不显示：确认 `@iconify/vue` 与图标集合名称
- 样式未生效：按需样式需先引入 `variables.css`

## 升级与版本
- 使用语义化版本（`MAJOR.MINOR.PATCH`）
- 文档更新建议独立分支与清晰提交信息
