# ScrollChips 滚动标签

## 安装

```ts
import { ZScrollChips } from 'clover-ui'
```

## 用法

```vue
<ZScrollChips v-model="value" :items="['A','B','C']" />
```

## 属性概览

- `items` 标签数组
- `modelValue` 选中值（`v-model`）
- `centerOnSelect` 选中后居中显示
- `snapCenter` 中心吸附
- `smoothScroll` 平滑滚动

